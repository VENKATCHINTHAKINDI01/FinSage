# FinSage AI — development and CI tooling
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Audited 2026-08-09 (FND-005).

-r requirements.txt

# ── Test ─────────────────────────────────────────────────────────────────────
pytest~=9.1.1
pytest-asyncio~=1.4.0
pytest-cov~=7.0
hypothesis~=6.165          # Layer 2 property invariants (CORE-012)
testcontainers[postgres,redis]~=4.10   # Layer 4 integration
schemathesis~=4.2          # Layer 4 OpenAPI contract fuzzing

# ── Static analysis ──────────────────────────────────────────────────────────
ruff~=0.16.2
mypy~=2.3.0
import-linter~=2.13        # enforces the backend/core purity contract

# ── Registry & supply chain ──────────────────────────────────────────────────
jsonschema~=4.26.0         # validates feature.json against feature.schema.json
pip-audit~=2.10.1
